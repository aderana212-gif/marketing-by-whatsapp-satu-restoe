name: Build Marketing By WhatsApp APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'app/**'
      - 'build.gradle'
      - 'settings.gradle'
      - 'gradle.properties'
      - '.github/workflows/build-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.7
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Generate Gradle wrapper
        run: gradle wrapper --gradle-version 8.7 --distribution-type bin

      - name: Fix mobile database card alignment
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('app/src/main/assets/index.html')
          s = p.read_text(encoding='utf-8')
          s = s.replace('.cats{grid-template-columns:1fr 1fr}.stats{grid-template-columns:1fr 1fr}', '.cats{grid-template-columns:repeat(3,1fr)}.cat{min-height:78px;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center}.stats{grid-template-columns:1fr 1fr}')
          p.write_text(s, encoding='utf-8')
          PY

      - name: Build release-named debug APK
        run: ./gradlew :app:assembleDebug --stacktrace

      - name: Verify and rename APK
        run: |
          test -f app/build/outputs/apk/debug/app-debug.apk
          cp app/build/outputs/apk/debug/app-debug.apk app/build/outputs/apk/debug/SatuRestoe_Marketing_WA_v1.1.apk
          ls -lh app/build/outputs/apk/debug/SatuRestoe_Marketing_WA_v1.1.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: SatuRestoe_Marketing_WA_v1.1
          path: app/build/outputs/apk/debug/SatuRestoe_Marketing_WA_v1.1.apk
          if-no-files-found: error
          retention-days: 30
